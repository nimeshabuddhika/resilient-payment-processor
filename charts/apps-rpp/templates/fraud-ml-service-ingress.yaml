{{- if .Values.fraudMlService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{.Values.fraudMlService.ingress.name}}
spec:
  ingressClassName: {{ .Values.fraudMlService.ingress.className }}
  {{- if .Values.fraudMlService.ingress.tls.enabled }}
  tls:
    - secretName: {{ .Values.fraudMlService.ingress.tls.secretName }}
      hosts:
        - {{ .Values.fraudMlService.ingress.host }}
  {{- end }}
  rules:
    - host: {{ .Values.fraudMlService.ingress.host }}
      http:
        paths:
          - path: {{ .Values.fraudMlService.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.fraudMlService.service.name }}
                port:
                  number: {{ .Values.fraudMlService.port }}
{{- end }}
