apiVersion: v1
kind: Service
metadata:
  name: haproxy-read
spec:
  type: {{ .Values.serviceTypes.haproxy }}
  selector: { app: haproxy-read }
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
