apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
  labels: { app: kafka-ui }
spec:
  type: {{ .Values.kafkaUi.service.type }}
  selector: { app: kafka-ui }
  ports:
    - name: http
      port: 8080
      targetPort: 8080
